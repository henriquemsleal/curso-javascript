function soma(n1, n2) { // parâm. pré definidos (n1=0, n2=0) recebe 0 se não informado
    return n1 + n2
}

let num1 = 10
let num2 = 15

console.log(`A soma dos números ${num1} e ${num2} é ${soma(num1, num2)}`)
